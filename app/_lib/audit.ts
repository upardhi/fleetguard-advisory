// Re-exported from app/api/_lib/audit.ts for backwards compatibility.
// The Admin SDK import lives exclusively inside app/api/ (safety rule S8).
export type { AuditPayload } from "../api/_lib/audit";
export { writeAuditLog } from "../api/_lib/audit";
